version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - ./fifa.db:/app/fifa.db
    environment:
      - DATABASE_URL=sqlite:///./fifa.db
      - DEBUG=true
    command: uvicorn backend.app.main:app --host 0.0.0.0 --port 8000 --reload
